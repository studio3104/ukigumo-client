require "spec_helper"

describe Ukigumo::Client do
  before :all do
    mock_server = "test.com"
    @ukigumo = Ukigumo::Client.new(mock_server)
  end

  context "post" do
  end

  context "search" do

  end
end
